#!/usr/bin/env node
'use strict';

console.log("SUM NODE'S DEPTHS");

function nodeDepths(root) {
    

}

class BinaryTree {
  constructor(value) {
    this.value = value;
    this.left = null;
    this.right = null;
  }
}

let A = new BinaryTree(1);
let B = new BinaryTree(2)
let C = new BinaryTree(3)
let D = new BinaryTree(2)
let B = new BinaryTree(2)
let B = new BinaryTree(2)
let B = new BinaryTree(2)
let B = new BinaryTree(2)
let B = new BinaryTree(2)
